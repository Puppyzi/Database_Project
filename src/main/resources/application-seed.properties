# Profile: seed
# Use this profile when you want to (re)load sample data from data.sql.
# Example: .\mvnw spring-boot:run -Dspring-boot.run.profiles=seed

# Run data.sql after Hibernate creates/updates tables
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
